<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.starward.mapper.MomentMapper">

    <select id="findAllMoments" resultType="top.starward.entity.Moment">
        SELECT id, content, mood, images_json, is_pinned, created_at, updated_at
        FROM moments
        ORDER BY is_pinned DESC, created_at DESC
    </select>

    <insert id="insertMoment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO moments (content, mood, images_json, is_pinned, created_at, updated_at)
        VALUES (#{content}, #{mood}, #{imagesJson}, #{isPinned}, NOW(), NOW())
    </insert>

</mapper>
